package net.craftal.web.mapper


object UserIdentityDataMapper {
  /*
  def transform(user: User): UserIdentity =
    UserAuthInfo(
      user.id,
      Option(user.email),
      Option(user.name),
      None,
      user.activated
    )
  */
}
